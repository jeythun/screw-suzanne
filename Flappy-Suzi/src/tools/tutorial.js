export default class Tutorial{
    constructor(container){
        this.toolbox = container;
        this.addTutorialModal();
    }

    addTutorialModal(){
        //creates tutorial button and adds it to the toolbox container
    }

    _createButton(){
        //creates and returns button that toggles tutorial screen

        //event listeners: click button to toggle modal visibility
    }

    _createModal(){
        //creates and returns tutorial screen element

        //show controls, gif, button to close

        //event listeners: click outside or button to close modal
    }

}